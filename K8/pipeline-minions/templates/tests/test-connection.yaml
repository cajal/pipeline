apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pipeline-minions.fullname" . }}-test-connection"
  labels:
    {{- include "pipeline-minions.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pipeline-minions.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
